

const ButtonManager = (props) => {

    // get current gallery, get button name
    // if the button name is equal to state gallery
    // img src = bright version
    const { currentGallery, buttonId } = props;

        if (currentGallery !== buttonId) {
            
        }

}

export default ButtonManager;